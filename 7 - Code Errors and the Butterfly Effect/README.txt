Project 7: Code Errors and the Butterfly Effect

Download version 3.8.2+ of Python 
Install pip
Create a new project. Within the terminal, use pip to install the libraries below:
----------------------
pip install numpy
pip install matplotlib
----------------------
Download the code and locate both Python file's directory

In the terminal, change the directory to the location
Run AnimatedLorenz.py

Close the command prompt, and change the directory to the location once more
Run main.py